/**
 * Bumped whenever a component is added, removed, or has a breaking prop
 * change — lets future tooling (or a future AI-assisted refactor) detect
 * "the design system changed under me" without diffing every file.
 */
export const DESIGN_SYSTEM_VERSION = '1.0.0';
